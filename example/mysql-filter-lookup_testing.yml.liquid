exec:
  max_threads: 1
  min_output_tasks: 1
in:
  type: mysql
  host: localhost
  user: root
  password: 'passsword'
  database: test
  table: customers
filters:
  - type: mysql_lookup
    host: localhost
    port: 3306
    database: test
    table_name: car
    username: root
    password: 'passsword'
    mapping_from:
    - car_name
    - company
    mapping_to:
    - model
    - brand
    new_columns:
     - { name: car_id, type: string }
     - { name: category, type: string }
     - { name: fuel_capacity, type: string }

out:
  type: mysql
  host: localhost
  user: root
  password: 'passsword'
  database: test
  table: output
  mode: truncate_insert
